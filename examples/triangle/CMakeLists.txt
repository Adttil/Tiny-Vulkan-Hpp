add_executable(triangle "triangle.cpp")
if(CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
    target_link_libraries(triangle PRIVATE tinyvkpp glfw stdc++exp)
else()
    target_link_libraries(triangle PRIVATE tinyvkpp glfw)
endif()